# Release Packages

Versioned release archives are generated here by `bun run release:package`.

The version number comes from the root `package.json`, and each archive has a matching JSON manifest with build time and source commit metadata.
